# QA Report: Story 2.2 -- Upload de Fotos de Comprovantes

**Reviewed By:** Quinn (Test Architect)
**Review Date:** 2026-03-28
**Gate Decision:** PASS

---

## 1. AC Coverage

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC-1 | Botao de upload no formulario de criacao/edicao | PASS | `ComprovantesUpload.tsx` integrates upload button in edit gasto page via `EditarGastoClient.tsx`. Label "Tirar Foto ou Selecionar Arquivo". |
| AC-2 | Mobile: camera + galeria | PASS | `ComprovantesUpload.tsx`: `<input type="file" accept={ACCEPT_STRING} capture="environment">`. `capture="environment"` triggers rear camera on mobile; without it, shows gallery+camera chooser. |
| AC-3 | Compressao client-side <= 200KB | PASS | `compress-image.ts` L14: `DEFAULT_MAX_KB = 200`. L66-147: `compressImage()` uses Canvas API with iterative JPEG quality reduction (0.7 down to 0.1 in steps of 0.1). L78: early return if already under limit. L91-99: scales to MAX_DIMENSION=1200px maintaining aspect ratio. PDFs bypass compression (L71-74). 17 unit tests pass. |
| AC-4 | Formatos: JPEG, PNG, WebP, PDF | PASS | `compress-image.ts` L24-29: `ACCEPTED_TYPES` Set with 4 types. L48-55: `validateFileType()` returns error for invalid types and enforces 10MB limit. L32: `ACCEPT_STRING` for input element. Tests verify BMP rejection. |
| AC-5 | Upload para bucket `comprovantes`; path `{empresa_id}/{gasto_id}/{timestamp}.{ext}` | PASS | `comprovante-actions.ts` L11: `BUCKET = 'comprovantes'`. L52-54: path built as `${usuario.empresa_id}/${gastoId}/${timestamp}.${ext}`. Migration 180600 L81-82: bucket creation SQL. |
| AC-6 | Registro em `foto_comprovante` + `foto_url` em gasto + icone na listagem | PASS | `comprovante-actions.ts` L69-79: INSERT into foto_comprovante with empresa_id, gasto_id, storage_path, content_type, size_bytes. L95-97: updates `gasto.foto_url` with storage_path. `GastoTable.tsx` renders paperclip icon when foto_url is present. |
| AC-7 | Remocao de comprovante; arquivo excluido do Storage | PASS | `comprovante-actions.ts` L107-176: `deleteComprovante()` fetches record, verifies empresa_id ownership, removes from Storage (L133-136), deletes DB record (L139-145), updates foto_url (sets to next remaining or null, L159-171). |
| AC-8 | Feedback visual: progress, sucesso, erro | PASS | `ComprovantesUpload.tsx`: progress bar with status text ("Comprimindo imagem..." / "Enviando..."), success message (green border), error message (red border). Status lifecycle: idle -> compressing -> uploading -> success/error. |
| AC-9 | Modal/lightbox ao clicar no icone | PASS | `ComprovantesUpload.tsx`: lightbox overlay (z-50, bg-black/80) with full-size image, close button. Also `ReceiptModal.tsx` (lazy loaded from GastoTable). |

**AC Coverage: 9/9 PASS**

---

## 2. Code Quality

| Check | Status | Details |
|-------|--------|---------|
| Strict TypeScript | PASS | `npx tsc --noEmit` passes clean for all Story 2.2 files. |
| Absolute imports (@/) | PASS | All imports use `@/` prefix. |
| ESLint | PASS | 0 errors, 0 warnings on Story 2.2 files. |
| Memory leak prevention | PASS | `compress-image.ts`: `URL.revokeObjectURL(url)` in success (L125), error (L119, L142), and canvas error (L107) paths. |
| Error handling | PASS | Structured `{success, error}` returns. Upload rollback on INSERT failure (L82-84). |
| Component naming | PASS | PascalCase: `ComprovantesUpload`, `ReceiptModal`. |
| Constants extracted | PASS | `MAX_DIMENSION`, `INITIAL_QUALITY`, `MIN_QUALITY`, `QUALITY_STEP`, `DEFAULT_MAX_KB` all as named constants. |

**Code Quality: PASS**

---

## 3. Build & Lint

| Command | Result |
|---------|--------|
| `npx tsc --noEmit` | PASS -- 0 errors on 2.2 files (3 pre-existing errors in unrelated files: fechamentos/actions.ts, configuracoes/combustivel/actions.ts, financeiro/historico/actions.ts) |
| `npx eslint` (story files) | PASS -- 0 errors, 0 warnings |
| `npx jest compress-image` | PASS -- 17/17 tests pass (0.396s) |

**Build & Lint: PASS**

---

## 4. Architecture

| Check | Status | Details |
|-------|--------|---------|
| Migration 180600 | PASS | Creates `foto_comprovante` table with correct schema. Indexes on gasto_id and empresa_id. |
| RLS on foto_comprovante | PASS | Migration L38: RLS enabled. 4 policies: dono/admin ALL (L41-46), motorista SELECT own gastos (L49-55), motorista INSERT own gastos (L57-64), motorista DELETE own gastos (L66-72). |
| Storage RLS | PASS | Migration L85-109: 3 policies on storage.objects for INSERT/SELECT/DELETE. All use `fn_get_empresa_id()::text` matched against first folder name. Bucket scoped by empresa. |
| Bucket naming | PASS | Uses `comprovantes` consistently throughout code and migration. |
| Server Action pattern | PASS | `comprovante-actions.ts` uses `'use server'`, FormData input, structured return types. |
| Signed URLs | PASS | `listComprovantes` L206-216: generates signed URLs with 1-hour expiry. Not using public URLs -- correct for private bucket. |
| Upload rollback | PASS | `uploadComprovante` L82-84: if DB insert fails, removes the uploaded file from Storage. |
| Delete cleanup | PASS | `deleteComprovante` L151-171: after deleting, checks remaining and updates/clears `foto_url` on gasto accordingly. |

**Architecture: PASS**

---

## 5. Security

| Check | Status | Details |
|-------|--------|---------|
| Authentication | PASS | All actions check `getCurrentUsuario()` first. |
| Empresa ownership (upload) | PASS | `uploadComprovante` L37-49: verifies gasto belongs to usuario's empresa before upload. |
| Empresa ownership (delete) | PASS | `deleteComprovante` L127-129: checks `comprovante.empresa_id !== usuario.empresa_id`. |
| Storage RLS | PASS | Triple policy: INSERT WITH CHECK, SELECT USING, DELETE USING -- all with `fn_get_empresa_id()`. Cross-empresa file access is impossible. |
| Table RLS | PASS | Motorista policies use subquery on gasto table with `fn_get_motorista_id()`. Dono/admin use `fn_get_empresa_id()` + role check. |
| No public URLs exposed | PASS | Bucket is private (L82: `public: false`). Signed URLs used for display. `foto_url` on gasto stores storage_path (not public URL). |
| File type validation | PASS | Both client-side (`validateFileType`) and server action validates contentType via FormData. |
| File size limit | PASS | `MAX_FILE_SIZE_BYTES = 10MB` (L35). Validated in `validateFileType` (L52-54). |

**Security: PASS**

---

## 6. Schema Alignment

### foto_comprovante

| Column (DDL) | Type (DDL) | Migration | Match |
|--------------|------------|-----------|-------|
| id | UUID PK | L9: UUID PRIMARY KEY DEFAULT uuid_generate_v4() | PASS |
| empresa_id | UUID NOT NULL | L10: UUID NOT NULL REFERENCES empresa(id) | PASS |
| gasto_id | UUID NOT NULL | L11: UUID NOT NULL REFERENCES gasto(id) ON DELETE CASCADE | PASS |
| storage_path | TEXT NOT NULL | L12: TEXT NOT NULL | PASS |
| thumbnail_path | TEXT | L13: TEXT | PASS |
| content_type | VARCHAR(50) | L14: VARCHAR(50) | PASS |
| size_bytes | INTEGER | L15: INTEGER + CHECK > 0 | PASS |
| uploaded_at | TIMESTAMPTZ | L17: TIMESTAMPTZ NOT NULL DEFAULT NOW() | PASS |
| created_at | TIMESTAMPTZ | L18: TIMESTAMPTZ NOT NULL DEFAULT NOW() | PASS |

**Schema Alignment: 9/9 PASS**

---

## 7. Prior QA Fixes Verification

| ID | Status | Details |
|----|--------|---------|
| SEC-001 | FIXED | `motoristas/actions.ts` L5: imports `requireRole`, L39: `requireRole(['dono', 'admin'])` in createMotorista. Also at L114, L163, L191, L221, L260 in other actions. |
| SEC-002 | FIXED | `caminhoes/actions.ts` L164-168: createCaminhao duplicate placa check includes `.eq('empresa_id', currentUsuario.empresa_id)`. L239-244: updateCaminhao also includes `.eq('empresa_id', currentUsuario.empresa_id)`. |
| MIG-001 | FIXED | `gasto.viagem_id` declared without FK in migration 180500 (L75: comment "FK para viagem adicionada na migration 20260328180700"). FK added via ALTER TABLE in migration 180700 (L120-121): `ALTER TABLE gasto ADD CONSTRAINT fk_gasto_viagem FOREIGN KEY (viagem_id) REFERENCES viagem(id) ON DELETE SET NULL;`. |

**Prior Fixes: 3/3 RESOLVED**

---

## Issues Summary

| ID | Severity | Finding | Suggested Action |
|----|----------|---------|------------------|
| (none) | -- | No blocking or notable issues found. | -- |

---

## Gate Decision

**Gate: PASS**

Rationale: Story 2.2 implementation is thorough and well-structured. Client-side compression with Canvas API correctly targets <= 200KB with iterative quality reduction. All 4 accepted formats (JPEG, PNG, WebP, PDF) are validated. Storage RLS uses `fn_get_empresa_id()` for empresa isolation on the bucket. Table RLS correctly separates dono/admin vs motorista access. Signed URLs prevent public file exposure. Upload rollback on failure is implemented. Memory leaks prevented with `URL.revokeObjectURL()` in all code paths. All 17 unit tests pass. TypeScript and ESLint clean. All 3 prior QA fixes (SEC-001, SEC-002, MIG-001) confirmed resolved.

---

*-- Quinn, guardiao da qualidade*
