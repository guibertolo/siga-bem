# Story 18.1: Campos de documentacao e IPVA no cadastro do caminhao

## Status
In Progress (branch `feat/story-18.1` criada a partir de main em 2026-04-13)

## Session Handoff (2026-04-13)

**Decisoes alinhadas com usuario:**
- Branch: `feat/story-18.1` (criada a partir de main, working tree limpo). NAO continuar em feat/copilot.
- Adicionar 5a coluna: `ipva_ano_referencia` (integer, nullable) — permite reset manual ano a ano + alerta automatico no ano seguinte.
- Badge alerta: 3 niveis (verde > 30 dias, amarelo 0-30 dias, vermelho < 0).

**Arquivos mapeados (nao implementado ainda):**
- Migration: criar `supabase/migrations/YYYYMMDDHHMMSS_add_ipva_fields_to_caminhao.sql`
- Types DB: `types/database.ts` (interface `Caminhao`)
- Types dominio: `types/caminhao.ts` (interfaces `Caminhao` + `CaminhaoFormData`)
- Schema Zod + Server actions: `app/(dashboard)/caminhoes/actions.ts` (linhas 11-53 schema, 139 create, 213 update)
- Form: `components/caminhoes/CaminhaoForm.tsx` (adicionar secao Documentacao)
- Upload reusavel: `components/gastos/ComprovantesUpload.tsx` (adaptar para bucket `comprovantes`, path `ipva/{caminhao_id}/`)
- Upload action referencia: `app/(dashboard)/gastos/comprovante-actions.ts` (valida 5MB, JPG/PNG/WEBP/PDF)
- Listagem: `components/caminhoes/caminhao-list.tsx` (badge 3 niveis)
- Wrappers: `components/caminhoes/CadastroCaminhaoClient.tsx` + `editar-caminhao-client.tsx`

**Retomar pela primeira task (Migration).**

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
```

## Story
**As a** dono de frota,
**I want** registrar vencimento do documento (CRLV), status do IPVA, valor pago e comprovante no cadastro de cada caminhao,
**so that** eu tenha controle completo da regularizacao da frota sem precisar de planilha externa.

## Epic
Epic 18 — Documentacao de Veiculos.

## Acceptance Criteria

1. Migration adiciona 5 colunas em `caminhao`:
   - `doc_vencimento` (`date`, nullable) — data de vencimento do CRLV
   - `ipva_pago` (`boolean`, default `false`) — se o IPVA do ano corrente foi quitado
   - `ipva_valor_centavos` (`integer`, nullable) — valor pago em centavos
   - `ipva_comprovante_url` (`text`, nullable) — URL do comprovante no Storage
   - `ipva_ano_referencia` (`integer`, nullable) — ano fiscal do pagamento (ex: 2026)
2. Formulario de edicao do caminhao exibe os 4 campos novos em secao "Documentacao".
3. Campo `doc_vencimento` usa date picker com mascara DD/MM/AAAA.
4. Campo `ipva_valor_centavos` usa mascara R$ (mesmo padrao dos gastos).
5. Upload do comprovante usa Supabase Storage (bucket `comprovantes`, path `ipva/{caminhao_id}/`).
6. RLS: mesmas regras do caminhao (dono/gestor da empresa).
7. Listagem de caminhoes mostra badge de alerta em 3 niveis:
   - Verde: `doc_vencimento` > hoje + 30 dias
   - Amarelo: entre hoje e hoje + 30 dias (vencendo)
   - Vermelho: `doc_vencimento` < hoje (vencido)
8. Testes unitarios para migration e validacoes.
9. `npm run lint && typecheck && test` passam.

## Dev Notes

- Apos implementar, o Copilot pode ganhar tool "documentos_caminhao_vencendo" (nova story).
- `ipva_pago` reseta anualmente? Decidir se manual ou automatico (sugestao: manual, dono marca quando paga).
- Comprovante: reusar logica de upload de `comprovante_url` dos gastos.

## Tasks / Subtasks

- [ ] Migration: adicionar 4 colunas
- [ ] Formulario: secao Documentacao
- [ ] Upload comprovante IPVA
- [ ] Badge alerta doc vencendo na listagem
- [ ] Testes
- [ ] Quality gates

## File List
_A preencher pelo @dev._

## Change Log
| Data | Versao | Autor | Descricao |
|------|--------|-------|-----------|
| 2026-04-12 | 0.1 | @sm (River) | Story criada a partir de feedback do usuario |
| 2026-04-13 | 0.2 | @dev | Handoff: branch `feat/story-18.1` criada, 5a coluna `ipva_ano_referencia` adicionada, badge 3 niveis, arquivos mapeados |
